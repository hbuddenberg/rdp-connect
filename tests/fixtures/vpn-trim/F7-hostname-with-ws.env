# Fixture F7 — surrounding-whitespace hostname (VPN_CHECK empty -> SKIP
# preflight). HOST trims to clean value. PASS_RDP padded with internal
# newline-equivalent literal whitespace to push the exclusion boundary.
HOST=  vpn.example.com 
USER_RDP=  user@example  
PASS_RDP=  p@ss w0rd  
DOMAIN=
VPN_CHECK=
PREFERRED_WS=  2  
LANG_OVERRIDE=  en  
