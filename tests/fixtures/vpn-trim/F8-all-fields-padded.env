# Fixture F8 — all 7 fields padded with surrounding whitespace. The
# comprehensive case: exercises every field in the trim allowlist (HOST,
# VPN_CHECK, DOMAIN, PREFERRED_WS, LANG_OVERRIDE) AND every field in the
# exclusion list (USER_RDP, PASS_RDP). Used by engine-security-delta
# scenario "trim_profile_fields() allowlist is the documented 5 trimmed +
# 2 excluded" (covered by tests/engine-security.bats in PR3).
HOST=  vpn.example.com  
USER_RDP=  user  
PASS_RDP=  pass with trailing space  
DOMAIN=  corp.local  
VPN_CHECK=  10.8.0.1  
PREFERRED_WS=  3  
LANG_OVERRIDE=  es  
