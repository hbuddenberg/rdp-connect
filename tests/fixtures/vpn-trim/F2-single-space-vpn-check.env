# Fixture F2 — single space VPN_CHECK (the bug: " " was treated as ENTER by
# the old non-trimmed code, producing "VPN requerida ( ) inalcanzable" on
# every invocation). Trim must yield empty -> SKIP preflight.
HOST=server.example.com
USER_RDP=user
PASS_RDP=secret
DOMAIN=
VPN_CHECK= 
PREFERRED_WS=
LANG_OVERRIDE=
