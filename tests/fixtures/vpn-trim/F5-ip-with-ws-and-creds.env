# Fixture F5 — surrounding-whitespace VPN_CHECK and HOST (the common
# copy-paste / editor mistake). Both fields trim to clean values. PASS_RDP
# and USER_RDP intentionally padded to assert the EXCLUSION invariant
# (credentials MUST NOT be trimmed).
HOST=  10.8.0.1  
USER_RDP=  corpuser  
PASS_RDP=  s3cret with spaces  
DOMAIN=
VPN_CHECK=  10.8.0.1  
PREFERRED_WS=
LANG_OVERRIDE=
