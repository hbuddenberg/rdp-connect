# Fixture F1 — empty VPN_CHECK (skip preflight, no surrounding whitespace).
# Verifies the original bug case (whitespace-only VPN_CHECK) PLUS the empty
# baseline. PASS_RDP/USER_RDP intentionally empty here (no credentials).
HOST=server.example.com
USER_RDP=
PASS_RDP=
DOMAIN=
VPN_CHECK=
PREFERRED_WS=
LANG_OVERRIDE=
