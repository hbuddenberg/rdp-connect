# Fixture F6 — clean values, no surrounding whitespace. Baseline for the
# "trim is idempotent" property: applying trim to already-clean values
# produces the same values.
HOST=10.8.0.1
USER_RDP=user
PASS_RDP=secret
DOMAIN=corp
VPN_CHECK=10.8.0.1
PREFERRED_WS=3
LANG_OVERRIDE=es
